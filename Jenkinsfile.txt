pipeline {
  agent any
  stages {
    stage('version') {
      steps {
	echo 'Stage Versions'
        sh 'python --version'
      }
    }
    stage('hello') {
      steps {
        sh 'python HelloWorld.py'
      }
    }
  }
}
